---
interface Props {
  href: string;
  label: string;
  variant?: 'primary' | 'secondary';
}

const { href, label, variant = 'primary' } = Astro.props;
---

<a href={href} class={variant === 'secondary' ? 'btn-secondary' : 'btn-primary'}>
  {label}
</a>
